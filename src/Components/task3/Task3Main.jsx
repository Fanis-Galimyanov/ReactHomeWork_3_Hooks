export default function Task3Main(){
    const array = [];
     
    function randArrGen(){
        for (let i = 0; i < 20; i++) {
            array.push(1 + Math.random() * ((100 - (-100)) + 1));
        }
    }
    return(

    );
}